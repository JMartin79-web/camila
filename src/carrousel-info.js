















// DETAILS
/*
const tourDetails = document.querySelector(".tour-details")
const tourSummary = document.querySelector(".tour-summary-p")
tourSummary.innerHTML = "Ver más +"

function handleDetails(){
    tourDetails.classList.toggle("tour-details-closed")
    if(tourDetails.classList.contains("tour-details-closed")){
        tourSummary.innerHTML = "Ver más +"
    }else{tourSummary.innerHTML = "Ver menos -"}
}
*/


